apiVersion: v1
kind: ConfigMap
metadata:
  name: epts-configs
  labels:
    name: epts-configs
  namespace: default
data:
  service-config.yml: |-
    kaa:
      applications:
        lamppost:
          versions:
            lamppost_v1:

    logging:
      level:
        org.kaaproject.cm: TRACE
